
I want to fix and complete the JAIDE Zig + Futhark model project so that a single Modal run of `scripts/modal_status_bench.py` produces a full, credible status benchmark of the model on a live NVIDIA B200 GPU, with success defined as **all six of the following passing in one Modal app execution with zero manual intervention**:

1. `zig build test-all -Doptimize=ReleaseSafe` exits 0 inside the Modal CPU container, including the C-API test (11/11) and every one of the 17 Zig test suites listed in `build.zig` (`test-tensor`, `test-memory`, `test-embedding`, `test-rsf`, `test-oftb`, `test-nsir`, `test-reasoning`, `test-crev`, `test-surprise`, `test-temporal`, `test-vpu`, `test-fnds`, `test-formal`, `test-security`, `test-quantum-adapter`, `test-signal`, `stress-refcount`) — no double-free, no memory leak, no refcount mismatch.
2. `zig build bench -Doptimize=ReleaseFast` exits 0 and every benchmark reports `RESULT: PASS`, including `bench-rsf` invertibility (currently `FAIL (invertibility check failed)`), with `bench-matmul` measured against the production `src/core/tensor.zig::matmul` (SIMD + tiled + multi-threaded), not the naive `src/optimizer/sfd.zig::matmul`.
3. `zig build -Dgpu=true -Doptimize=ReleaseFast` exits 0 and produces both binaries in `zig-out/bin/`: `jaide-distributed-futhark` and `jaide-inference-server`. Zero `undefined symbol` linker errors. Zero "expected N argument(s), found M" compile errors.
4. Phase C (mini training on B200) runs `jaide-distributed-futhark` with `MODEL_DIM=512, NUM_LAYERS=8, BATCH_SIZE=4, EPOCHS=3, SAMPLE_CAP=1000, MAX_SEQ_LEN=256, LEARNING_RATE=0.0001` against the downloaded `HuggingFaceFW/finephrase` dataset (config `faq`), writes an epoch-by-epoch loss curve to `/reports/run_<id>/phase_c_training.log` and a machine-parseable `/reports/run_<id>/training_metrics.json` containing `{"epochs":[{"epoch":n,"loss":f,"grad_norm":f,"lr":f,"tokens_per_sec":f,"gpu_mem_mib":i}...], "converged": true|false, "first_loss": f, "last_loss": f}`. Converged means `last_loss < first_loss * 0.95`.
5. Phase D (inference smoke test) starts `jaide-inference-server` on the same B200 container, issues at least one real HTTP inference request via loopback, receives a non-empty generated token stream, records latency and the full JSON response body to `/reports/run_<id>/phase_d_inference.json`, and shuts down cleanly.
6. The top-level Modal entrypoint prints a final aggregated `FINAL SUMMARY` JSON containing every phase's `returncode`, `duration_s`, key metrics, and paths to every artifact written to the `jaide-bench-reports` Modal volume; the command `modal volume get jaide-bench-reports /run_<id>/summary.json ./` retrieves this file after the run.

Write full, unabridged, production-ready code in every file touched. Never write comments in code. Never write partial files, ellipses, `// ...` elisions, `unchanged` markers, TODOs, `sorry`, mock, placeholder, dummy, simulated, or fake logic. Every file listed below must be rewritten in its entirety when modified, or created in full when new. Communicate progress in Hungarian.

Execute the following work in this exact order.

**Step 1 — Read current state.** Run `zig build test-all -Doptimize=ReleaseSafe 2>&1 | tail -100` and `zig build -Dgpu=true -Doptimize=ReleaseFast 2>&1 | tail -100` locally (or read the latest Modal run log via `modal app logs <app-id> --profile=kutyamajom`). Enumerate every current compile error, linker error, and test failure. Do not proceed to any code change until the current failure set is fully catalogued.

**Step 2 — Fix `src/tests/bench_rsf.zig` invertibility failure.** Read the file end-to-end. The failure `RESULT: FAIL (invertibility check failed)` means `‖x - backward(forward(x))‖ / ‖x‖ > tolerance`. Determine whether the bug lives in `src/processor/rsf.zig` (`syncAllLayersGPU`, `forward`, `backward`), in the tolerance value, in the block-diagonal Jacobian construction, in the fixed-point iteration convergence check inside RSF, or in the test's expected round-trip assumption. Fix the actual source of non-invertibility. Never loosen the tolerance to hide the bug. If the RSF layer uses spectral normalization to ensure invertibility, verify the power-iteration converges below the required Lipschitz constant `L < 1`; if not, increase power iterations, tighten the sigma clamp, or use exact SVD for small dims. Rewrite the affected files in full. The test must pass with tolerance ≤ 1e-4 on all four `dim=512, layers=12, batch=64` iterations.

**Step 3 — Fix `src/tests/bench_matmul.zig` to measure the production matmul.** The current bench imports `deps.sfd.Tensor` whose `matmul` is a naive `f64` scalar triple loop, giving 3.5 GFLOPS. Rewrite `src/tests/bench_matmul.zig` in full to import `const Tensor = deps.core.Tensor;` (the SIMD + tiled + multi-threaded implementation at `src/core/tensor.zig::matmul`). Ensure `src/_bench_deps.zig` re-exports `core` with the correct `Tensor` symbol. Update call sites from the self-mutating signature `try c.matmul(&a, &b);` to the actual signature `var c = try Tensor.matmul(&a, &b, allocator); defer c.deinit();`. Keep warmup=10, iters=100, sizes=[128,256,512,1024]. Expected result on Modal with 4-8 core burst: ≥ 30 GFLOPS at 1024×1024. Print CPU core count actually used at the top of the bench output.

**Step 4 — Fix `src/optimizer/sfd.zig::matmul` performance.** Rewrite the entire `src/optimizer/sfd.zig` file. Replace the naive scalar `f64`-cast triple loop in `matmul` with the same production algorithm used in `src/core/tensor.zig::matmul`: allocate a transposed copy of B, block by 32, use `@Vector(8, f32)` SIMD reduction inside the k-loop, spawn `std.Thread` workers up to `min(getCpuCount(), min(m, 8))`. Keep the aliasing check (self.data.ptr overlap with A or B → route through temporary). Preserve every other function in `sfd.zig` verbatim (spectral filter decomposition, FP4 quantization, SpectralNorm, sigma computations); only `matmul` gets the rewrite. Add nothing new to the public API.

**Step 5 — Fix `src/core/tensor.zig` matmul thread count.** The current line `const thread_count = @min(@max(std.Thread.getCpuCount() catch 1, 1), @min(m, 8));` on Modal reads the host CPU count (48+), not the container's cgroup slice (2 guaranteed / 8 burst). Rewrite `src/core/tensor.zig` in full. Add a helper `effectiveCpuCount()` that reads `/sys/fs/cgroup/cpu.max` (cgroup v2) or `/sys/fs/cgroup/cpu/cpu.cfs_quota_us` + `cpu.cfs_period_us` (cgroup v1), falls back to `std.Thread.getCpuCount()` on non-Linux or read failure, clamps to `[1, 8]`, and further clamps to `min(m, threads)`. Also add a `cgroupSource()` helper returning `"cgroup_v2"`, `"cgroup_v1"`, or `"fallback"` for observability. Use `effectiveCpuCount()` in `matmul` and any other multi-threaded op. No comments.

**Step 6 — Fix `src/api/inference_server.zig` compilation.** Read the file end-to-end. Scan for every init call whose signature was recently changed. Every `foo.init(...)` that returns `!T` must be inside a `try` or captured with `if (...) |value| ... else |err| ...`. Every `foo.init(...)` whose current call site passes the wrong argument count must be corrected. Specifically ensure `SecurityProofEngine.init(alloc, SecurityLevel.INTERNAL)` uses two arguments with `try` wrap, and `FormalVerificationEngine.init(alloc)` uses `try` wrap. Add `SecurityLevel` import from `core_relational` if missing. Rewrite the entire file when structural changes are needed. The final binary must compile with `zig build -Dgpu=true -Doptimize=ReleaseFast` without errors.

**Step 7 — Fix `src/distributed/distributed_trainer_futhark.zig` training loop output.** The current run produced no visible training output on B200 — the process was launched and cancelled without emitting a loss curve. Read the file end-to-end. The training loop must, for every step and every epoch, print to stdout a line of the exact format:
```
[train] epoch=<n> step=<s>/<total> loss=<f> grad_norm=<f> lr=<f> tokens_per_sec=<f> gpu_mem_mib=<i> elapsed_s=<f>
```
and at epoch end write one JSON line per epoch to `std.posix.getenv("JAIDE_METRICS_PATH") orelse "/reports/training_metrics.jsonl"`, format:
```
{"epoch":<n>,"loss":<f>,"grad_norm":<f>,"lr":<f>,"tokens_per_sec":<f>,"gpu_mem_mib":<i>,"elapsed_s":<f>}
```
Flush stdout after every step. At program exit, write `/reports/training_metrics.json` (the aggregated array + `converged` bool + `first_loss` + `last_loss` fields). Rewrite the entire file. Preserve every existing feature: `TrainerComponents`, `initWithComponents`, VPU+FNDS wiring, `runCoreRelationalPass`, `buildKnowledgeGraph`, `GradientFlowController`, heap-allocated `signal_engine` and `nsir_graph`, `CHECKPOINT_MAGIC`/`CHECKPOINT_TRAILER` atomic save and staged transactional load. Environment variables the loop must read: `MODEL_DIM`, `NUM_LAYERS`, `BATCH_SIZE`, `EPOCHS`, `SAMPLE_CAP`, `MAX_SEQ_LEN`, `LEARNING_RATE`, `JAIDE_DATASET_PATH`, `JAIDE_CHECKPOINT_DIR`, `JAIDE_METRICS_PATH`, `JAIDE_REPORT_DIR`. Defaults defined once at the top of the file.

**Step 8 — Fix `src/inference_server_main.zig` to bind, serve, and terminate cleanly.** Read the file end-to-end. Ensure the server binds to `0.0.0.0:$JAIDE_INFERENCE_PORT` (default 8080), loads the checkpoint from `$JAIDE_CHECKPOINT_DIR/final.ckpt`, handles at least the routes `GET /health` (returns `{"status":"ok","model_loaded":true}`) and `POST /v1/generate` (accepts `{"prompt": "...", "max_tokens": n}` and returns `{"generated": "...", "tokens": [...], "latency_ms": f, "prompt_tokens": i, "completion_tokens": i}`), and shuts down on SIGTERM within 5 seconds. Rewrite the entire file. No mock inference — the response must come from the loaded model's actual forward pass through the full VPU/FNDS/CREV/FormalVerifier/SecurityProof/QuantumAdapter pipeline.

**Step 9 — Fix `build.zig` for GPU build correctness.** Read the file end-to-end. Ensure two distinct `Options` objects exist: `cpu_build_options` (`gpu_acceleration=false` always) and `gpu_build_options` (`gpu_acceleration=gpu_enabled`). `inference_server_exe` uses `cpu_build_options` and links only `futhark_kernels.c`. `distributed_futhark_exe` (only built when `gpu_enabled`) uses `gpu_build_options` and links `futhark_kernels.c` + `main_gpu.c` + system libraries `cuda`, `cudart`, `nvrtc`, `nccl`, `m`, `pthread`, `dl`. `futhark_gpu_step` uses `futhark cuda --library` (not `opencl`). All four bench executables (`bench-rsf`, `bench-matmul`, `bench-tensor-ops`, `bench-sfd`) use `cpu_build_options`. All 17 test targets use `cpu_build_options`. Rewrite the entire `build.zig`.

**Step 10 — Fix `src/hw/accel/futhark_bindings.zig` completeness.** Every Futhark CUDA runtime function called anywhere in the codebase must be either declared inside the `gpu_stub` conditional struct (real `extern "c"` when `_build_gpu_enabled`, no-op inline otherwise) or declared unconditionally at the top level as a shared symbol available in both CPU and CUDA backends. There must be no direct call from any `.zig` file to a `futhark_context_config_set_*` function outside `gpu_stub.*`. Run `grep -rn "futhark_context_config_set_" src/ | grep -v futhark_bindings.zig`. Every hit must be rewritten to `futhark.gpu_stub.set_device(...)` style. Rewrite each affected file in full. Fix `src/processor/rsf.zig::syncAllLayersGPU` if it directly calls the raw functions.

**Step 11 — Fix Futhark warnings that indicate real bugs.** Read `src/hw/accel/main.fut` and `src/hw/accel/futhark_kernels.fut`. For every `Warning at ...: Unused name "..."` reported (`g_in`, `gw_s`, `gw_t`, `gs`, `gt`, `gin`, `weights`, `edge_targets`, and any others), inspect the surrounding function. If the parameter was intended to contribute to the computation and the bug is that it was dropped from the body, use it correctly. If the parameter is genuinely unused in this specialization, prefix with `_` to silence the warning. A dropped `weights` parameter in a gradient function is a real bug — fix the gradient formula. Rewrite the affected `.fut` files in full. No suppression flags.

**Step 12 — Add cgroup-aware CPU count printing to every benchmark.** Rewrite each of `src/tests/bench_matmul.zig`, `src/tests/bench_tensor_ops.zig`, `src/tests/bench_sfd.zig`, `src/tests/bench_rsf.zig` in full. Add at the top of each `main`:
```
const eff = core.effectiveCpuCount();
const src = core.cgroupSource();
std.debug.print("[env] effective_cpu={d}  cgroup_source={s}\n", .{ eff, src });
```
Where `effectiveCpuCount` and `cgroupSource` are exported from `src/core/tensor.zig`. This makes it visible in Modal logs how many cores the container actually got.

**Step 13 — Rewrite `scripts/modal_status_bench.py` in full.** Preserve the CPU/GPU function split. Apply the following changes:
- `prepare_cpu` container CPU allocation: raise from `cpu=(2.0, 8.0)` to `cpu=(4.0, 16.0)` so matmul multi-threading has room; keep `memory=(4096, 16384)`.
- After `zig build -Dgpu=true` succeeds, copy both `zig-out/bin/jaide-distributed-futhark` and `zig-out/bin/jaide-inference-server` to `/build_artifacts/run_<id>/`.
- `run_gpu_train_and_infer`: after Phase C (training), read `/reports/run_<id>/training_metrics.json`, verify `converged == true`, and record the result in the phase result dict. If not converged, mark the phase result as `WARN` but continue to Phase D.
- Phase D: launch `jaide-inference-server` as a background subprocess with `JAIDE_INFERENCE_PORT=8080`, wait up to 30s for `curl -f http://127.0.0.1:8080/health` to succeed, then POST `{"prompt": "Explain the JAIDE architecture in one sentence.", "max_tokens": 64}` to `/v1/generate`, capture the full JSON response body, save to `/reports/run_<id>/phase_d_inference.json`, send SIGTERM, wait up to 10s for exit.
- Final summary JSON must include every phase's `returncode`, `duration_s`, key metric extracts (`training_first_loss`, `training_last_loss`, `training_converged`, `inference_latency_ms`, `inference_generated_text`, `gpu_name`, `gpu_memory_used_mib_peak`), and full paths to every artifact written to the `jaide-bench-reports` volume. Write it to `/reports/run_<id>/summary.json` and also print it to stdout with clear delimiters.
- Add a `stop-on-error` flag `JAIDE_STOP_ON_PHASE_FAIL` (default `false`): if any of A / A2 / B / C / D returns non-zero and this env var is `true`, abort the run and skip subsequent phases.
- Add SIGTERM handler in the entrypoint so `modal app stop <id>` cleanly terminates the training and inference subprocesses without leaving zombie GPU processes.

**Step 14 — Verify all changes locally first.** Before triggering a Modal run, execute in the local sandbox (or Replit shell): `zig build test-all -Doptimize=ReleaseSafe`, `zig build bench -Doptimize=ReleaseFast`, `zig build -Dgpu=true -Doptimize=ReleaseFast`. Read every reported error, iterate until all three exit 0. GPU binaries will link against CUDA stubs locally — that is acceptable, the smoke test is that the build reaches `install` step.

**Step 15 — Trigger and monitor Modal run.** Execute `modal run --detach scripts/modal_status_bench.py --profile=kutyamajom`. Capture the returned `ap-<id>`. Follow with `modal app logs ap-<id> --profile=kutyamajom --follow` until completion. On success, retrieve every artifact from the report volume: `modal volume get jaide-bench-reports /run_<id>/summary.json ./` and inspect its contents. Confirm all six success criteria are met.

**Step 16 — Report back.** Produce a single Hungarian summary containing: the Modal `ap-<id>`, the six success-criterion check results (each ✅ or ❌), the training loss curve first/last values with `converged` bool, the inference response body verbatim, the total Modal GPU spend estimate in USD (B200 at ~$6-8/h × elapsed GPU minutes), and paths to every artifact.